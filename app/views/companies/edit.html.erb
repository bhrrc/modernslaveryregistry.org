<div class="container">
  <div class="section">
    <h2 class="title is-5"><%= @company.statements.any? ? 'Submit Statement' : 'Submit Company' %></h2>
    <%= render partial: 'company_form', locals: { company: @company } %>
  </div>
</div>
